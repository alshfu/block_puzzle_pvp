name: block_puzzle_pvp
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # --- Firebase & Authentication (Совместимые версии) ---
  firebase_core:
  firebase_auth:
  cloud_firestore:
  google_sign_in:
  sign_in_with_apple:

  shared_preferences: ^2.2.3
  crypto: ^3.0.3
  # ИСПРАВЛЕНИЕ: Установлена версия, совместимая с flutter_localizations.
  intl: ^0.20.2
  cupertino_icons:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
  uses-material-design: true
  generate: true
